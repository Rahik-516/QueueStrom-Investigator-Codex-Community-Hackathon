# --- Core API ------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.32.1

# --- Validation ----------------------------------------------------------
pydantic==2.10.3

# --- LLM orchestration ---------------------------------------------------
# `instructor` is the schema-enforcing wrapper.
# We pin a version that supports both OpenAI and Anthropic via the unified
# `client.chat.completions.create(..., response_model=...)` API.
instructor==1.5.2
openai==1.82.0
anthropic==0.39.0

# --- Operational niceties (no runtime side-effects) ----------------------
python-dotenv==1.0.1 ; python_version < "4.0"
